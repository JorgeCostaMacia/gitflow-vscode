# Normalize line endings to LF in the repository for all text files.
# Prevents CRLF/LF diff noise when working across Windows and Unix.
* text=auto eol=lf

# Binary files git should never touch.
*.vsix binary
*.png binary
*.svg text eol=lf
